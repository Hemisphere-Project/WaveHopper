{
  "name": "wavehopper",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot server/relay.ts",
    "start": "bun run server/relay.ts",
    "build:stations": "bun run server/build-stations.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
