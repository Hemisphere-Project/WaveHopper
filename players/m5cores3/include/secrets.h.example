// Copy to secrets.h (gitignored) and fill in. First boot stores these to NVS;
// afterwards NVS wins, so a rebuild with different values only applies after
// the NVS copy is cleared (or when NVS has no credentials yet).
#pragma once

#define WH_WIFI_SSID "your-ssid"
#define WH_WIFI_PASS "your-password"
